{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run",
            "type": "shell",
            "command": "meson compile -C builddir && meson install -C builddir && builddir/install/bin/longlens",
            "problemMatcher": []
        }
    ]
}